import { colors } from "@/App";

export const Settings = () => {
  return (
    <div className={`flex justify-center items-center w-screen py-5 ${colors['text-white']}`}>
      <div className="">test</div>
    </div>
  );
}